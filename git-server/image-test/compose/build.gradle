
plugins {
   id 'com.palantir.docker-compose' version '0.34.0'
}


apply from: "$rootProject.projectDir/git-server/project.cfg"
apply from: "$rootProject.projectDir/git-server/image/project.cfg"
apply from: "$rootProject.projectDir/git-server/image-test/image/project.cfg"

def registry = registry_name
// gitServer_imageName
// gitServer_imageVersion
// gitClient_imageName
// gitClient_imageVersion



dependencies {
   docker 'kineticfire:git-server:1.2.0'
   docker 'kineticfire:git-server-test-client:1.0.0'
}


dockerCompose {
   dockerComposeFile 'docker-compose.yml'
}
